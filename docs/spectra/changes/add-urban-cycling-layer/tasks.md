## 0. Prerequisite

- [x] 0.1 Confirm change `prioritize-riverside-stations` is applied: `isRiversideRoute` exists in its scripts/lib/ module and `public/data/stations.json` records carry `riverside`. Verify: `npm test` passes and `node -e` over stations.json finds the field. If it is not applied, stop and apply that change first.

## 1. Urban cycling dataset

- [x] 1.1 Urban cycling dataset, classification — Exclude riverside route members from the urban layer and apply the rule for Signals and crossings within 30 m of urban paths: add a module under scripts/lib/ with the element classifier and a builder that takes an Overpass response plus riverside member-way ids and returns `{ paths, points }` in the One cycling.json with compact arrays shape (sorted by OSM id, 5-decimal coordinates). Verify: Vitest tests for every row of the "element mapping" table and the rounding example in the supply-data delta spec pass under `npm test`. [after: 0.1]
- [x] 1.2 Guard urban cycling counts: `checkCounts` gains `cyclingPaths: 1000` and `cyclingPoints: 2000`. Verify: new cases in scripts/lib/check-counts.test.js assert that 300 paths yields one message naming urban cycling paths and 300, and that counts at the minimums yield no message. [after: 0.1]
- [x] 1.3 The pipeline runs the urban cycling query through the existing Overpass fallback, writes `public/data/cycling.json` one record per line, adds `counts.cyclingPaths` and `counts.cyclingPoints` to meta.json, and publishes nothing when a guard fails. Verify: `npm run fetch-data` succeeds and prints both counts; `node -e` confirms cycling.json has `paths` and `points` arrays with the expected kinds. [after: 1.1, 1.2]

## 2. Map layer

- [x] 2.1 Urban bike-path layer toggle and Lazy-load cycling.json on first toggle: MapControls.vue gains a `v-model:show-cycling` switch labelled `都市自行車道`, off by default; index.vue fetches `data/cycling.json` only on the first enable through a loader under app/utils/, keeps it afterwards, and on failure shows `自行車道資料載入失敗` and resets the toggle. Verify: a Vitest test of the loader asserts it rejects on HTTP 404, and the manual check in 2.4 passes. [after: 1.3]
- [x] 2.2 Urban bike paths are drawn with Canvas rendering for paths and points: SupplyMap.client.vue draws cycleways as solid lines and lanes as dashed lines on one canvas renderer, non-interactive, added and removed with `showCycling`, plus a legend control shown only while the layer is on. Verify: `npx nuxi typecheck` reports no errors and the manual check in 2.4 passes. [after: 2.1]
- [x] 2.3 Traffic signals and crossings along urban paths: signal and crossing markers in the canvas renderer, in a layer group present only while the layer is on and zoom is 16 or greater, updated on `zoomend`. Verify: the manual check in 2.4 passes. [after: 2.2]
- [x] 2.4 Manual check in `npm run dev`, desktop and 375 px: no cycling.json request on load in the browser network panel; turning on draws solid and dashed lines and the legend; markers show at zoom 16 and hide at zoom 15; turning off removes lines, markers, and legend; a station under a line is still selectable; with cycling.json renamed away, turning on shows `自行車道資料載入失敗` and the toggle returns to off. Record results in the task notes. [after: 2.3]
  - Result (2026-09-25, `npm run dev`, Chrome): on load only meta.json, stations.json and shops.json were requested; cycling.json was requested only after turning the switch on. Legend showed 自行車道 / 自行車道（畫線）/ 紅綠燈 / 穿越道; solid and dashed green lines visible (e.g. 民權東路 solid, 松江路 dashed). Signal-red canvas pixels: 0 at zoom 15, 40093 at zoom 16. Real click on 開元市民活動中心, which sits on a bike-path line, selected it (19 shops); all 28 visible station markers were topmost at their centre. Switch off → legend gone, green and red canvas pixels 0, selection kept. With cycling.json renamed away (HTTP 404) → 「自行車道資料載入失敗」, switch back off, 34 riverside clusters still shown; after restoring the file, turning it on loaded the layer. 375 px iframe: scrollWidth 360, both switches 32 px tall, legend (142×85) inside the 360×429 map.

## 3. Docs and release

- [x] 3.1 README feature list and data-source table describe the `都市自行車道` layer, what counts as an urban bike path, that sidewalk shared paths are excluded, and that data comes from OSM. Verify: content review of README.md. [after: 2.4]
- [x] 3.2 Generated `public/data/cycling.json` ships in the same commit as the UI change. Verify: `npm test` and `npm run generate` pass, and `git show --stat` lists public/data/cycling.json together with the app/ changes. [after: 3.1]
