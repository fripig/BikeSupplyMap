## 1. Unified station style

- [x] 1.1 Update Map shows riverside stations by default: deliver "Both kinds share one station style" and "Selected station stands out" with one bicycle divIcon for both kinds, a selected station that is a larger, darker variant, and one cluster style for both groups, using the colors table in design.md: in SupplyMap.client.vue add `BIKE_SVG`, make riverside and urban markers `station-icon station-icon--riverside|--urban` 18 px with the SVG, the selected marker 26 px `#e67700` with the SVG, a shared `iconCreateFunction` producing `station-cluster station-cluster--riverside|--urban` 32 px orange circles for both groups, and drop the gray urban styles; remove `MarkerCluster.Default.css` from nuxt.config.ts. Write the e2e test "station markers and clusters of both kinds share one style" first (compare computed size, background, and border of one riverside and one urban marker and cluster, check each marker has an `svg`, no `.marker-cluster` exists, and the selected marker is wider with a different background), then move the existing `urbanMarkers` / `riversideMarkers` locators in e2e/map.spec.ts to the new classes. Verified by `npx vue-tsc --noEmit` and `npm run test:e2e`.
- [x] 1.2 [after: 1.1] Update README.md so the station description no longer says non-riverside stations are gray, and describes the bicycle icon and shared cluster style. Verified by reading the README paragraph on the two switches and running `grep -n 灰色標記 README.md` with no match.
