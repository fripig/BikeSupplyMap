## 1. Rain shelter dataset

- [x] 1.1 Implement bridge spots from segment crossings merged at 60 m: scripts/lib/shelters.js exports `bridgeSpots(routes, bridgeWays)` and `BRIDGE_SPOT_MERGE_METERS = 60`, using only `kind: "riverside"` route lines, keeping only ways that match the bridge filter (the listed `highway` or `railway` values with `bridge=yes`), merging crossings in way-id then along-way order, naming each spot from the first named way, rounding to 5 decimals and sorting by lat then lng. Verified by scripts/lib/shelters.test.js cases for every row of the spec's "crossing merge" and "bridge way selection" examples plus a bridge or link route line being ignored (`npx vitest run scripts/lib/shelters.test.js`).
- [x] 1.2 [after: 1.1] Implement shelter spots within 100 m of riverside lines: `shelterSpots(routes, elements)` and `SHELTER_NEAR_METERS = 100` in scripts/lib/shelters.js select `amenity=shelter` (not `shelter_type=public_transport`) and `building=roof` elements by node position or center, reuse `isNearSegments()` from scripts/lib/riverside.js, deduplicate by shop-style id (`n123`, `w456`), and sort by that id as a string. Verified by shelters.test.js cases for every row of the spec's "shelter selection" example (`npx vitest run scripts/lib/shelters.test.js`).
- [x] 1.3 [after: 1.2] Wire the separate shelters.json built by a shelter query into scripts/fetch-data.js: add `SHELTERS_QUERY` (bridge ways with `out geom`, shelters and roofs with `out center tags`) run after the urban cycling query; write `shelters.json` as bridge spots then shelter spots, one entry per line; add `counts.shelters = { bridge, shelter }` to meta.json; add shelter minimums in check-counts (`bridgeShelters: 80`, `shelters: 80`, labels `bridge shelter spots` and `shelter spots`) in scripts/lib/check-counts.js; log both counts. In scripts/fetch-data.test.js the fake server currently sends any Overpass query without `"route"="bicycle"` or `"highway"="cycleway"` to the shop response, so it gains a `/shelters` branch matched on `"amenity"="shelter"`, and the SEED gains `shelters.json`. The existing fixture's riverside way 1 spans about 2 km (121.53 → 121.55 at lat 25.03), too short for 80 bridge spots 60 m apart, so the shelter fixture needs a riverside line of at least 5 km (lengthen way 1 or add a riverside relation) and every assertion that depends on the changed fixture is updated. Verified by fetch-data tests for: output order and counts, meta `counts.shelters`, rejection with 40 bridge spots naming `bridge shelter spots: got 40`, and previous data kept when the shelter query fails (`npx vitest run scripts`).

## 2. Rain shelter layer on the map

- [x] 2.1 Add a generic lazy switch for the shelter layer: `useLazyToggle<T>(load, initiallyOn)` in app/utils/cycling.ts with `useCyclingToggle` as a wrapper passing `true`, and `createJsonLoader<T>(fetchFn, baseURL, file)` in app/utils/load-data.ts with `createCyclingLoader` as a wrapper; add the `Shelter` and `ShelterData` types. Verified by app/utils/cycling.test.ts cases for `useLazyToggle` starting off (no load before switch-on, one load after on/off/on, retry after failure with the switch turned back off) and the existing `useCyclingToggle` and `createCyclingLoader` tests unchanged and passing (`npx vitest run app/utils`).
- [x] 2.2 [after: 2.1] Add the Rain shelter switch: MapControls.vue gains a `showShelters` model and `shelterFailed` prop rendering a `躲雨點` switch and `躲雨點資料載入失敗` under it only on failure; app/pages/index.vue creates the shelter toggle off by default with `createJsonLoader(fetch, app.baseURL, 'shelters.json')`, starts it on mount without fetching while off, and passes `showShelters` and `shelters` to SupplyMap. Verified by app/components/MapControls.test.ts cases for the label, default off, emitted value, and failure message (`npx vitest run app/components`).
- [x] 2.3 [after: 2.2] Draw Rain shelter icons as rounded square glyph icons below stations and extend the legend of "Traffic signals and crossings along urban paths": `shelterLabel(kind, name)` and the fourth `sheltersShown` parameter of `legendEntries()` in app/utils/cycling.ts; SupplyMap.client.vue draws `shelter-icon` divIcons (`橋` / `亭`, background `#495057`, `zIndexOffset: -1000`) with popups from `shelterLabel`, adds or removes the layer with the switch, and styles the two legend glyphs. Verified by cycling.test.ts cases for every row of the "shelter popup text" example and every row of the updated "legend entries" example (`npx vitest run app`), and `npx vue-tsc --noEmit` passing.

## 3. Data, browser checks and docs

- [x] 3.1 [after: 1.3] Regenerate data with `npm run fetch-data`; the run succeeds and public/data/shelters.json holds at least 80 `bridge` and 80 `shelter` entries, with a `bridge` entry named `中正橋`. Verified by the command output and a node check of shelters.json.
- [x] 3.2 [after: 2.3, 3.1] Add e2e tests in e2e/map.spec.ts: no request for `data/shelters.json` at load; turning `躲雨點` on draws shelter icons and adds 橋下躲雨點 and 涼亭躲雨點 to the legend; clicking the icon of a named bridge entry from shelters.json shows `<name> · 橋下`. Verified by `npm run test:e2e` passing on desktop and phone.
- [x] 3.3 [after: 3.1] Document the 躲雨點 layer in README.md: what the two kinds are, that the switch is off by default, the selection rules and their module, the new fetch-data minimums, and that toilets are not included. Verified by reading the README diff against the rain shelter dataset requirement.
