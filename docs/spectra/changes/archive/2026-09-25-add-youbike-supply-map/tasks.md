## 1. Project setup

- [x] 1.1 Scaffold the project per "Nuxt static generation with client-only Leaflet": Nuxt 4 app with `app/` directory, package.json scripts `dev` (nuxt dev), `generate` (nuxt generate), `preview`, `test` (vitest run), `fetch-data`; dependencies nuxt, leaflet, leaflet.markercluster, vitest; `nuxt.config.ts` with `app.baseURL: '/BikeSupplyMap/'` and Nitro preset `github_pages`; `node_modules/`, `.nuxt/`, `.output/` added to .gitignore. Verify: `npm install` succeeds, `npm run generate` emits `.output/public/index.html` whose asset URLs start with `/BikeSupplyMap/_nuxt/`, and `npx vitest run --passWithNoTests` exits 0.

## 2. Data pipeline (supply-data)

- [x] 2.1 [after: 1.1] Implement `normalizeTaipei` and `normalizeNewTaipei` in `scripts/lib/normalize-stations.js` so that the "Station dataset covers both cities" field mapping holds: `YouBike2.0_` prefix removed, string coordinates converted to numbers, `act` other than `"1"` returns `null`. Verify: vitest cases for every row of the spec's "source field mapping" example table pass.
- [x] 2.2 [after: 1.1] Implement `classifyShop` in `scripts/lib/classify-shop.js` following "Brand-first shop classification" and the "Shop classification rules" requirement (exclusion → brand → shop-tag fallback; way/relation use `center`; id prefixed with `n`/`w`/`r`; missing name → `null`). Verify: a table-driven vitest test covering every row of the spec's classification table plus the way-center scenario passes.
- [x] 2.3 [after: 2.1, 2.2] Implement `scripts/fetch-data.js` (`npm run fetch-data`) so the "Shop dataset covers both cities" and station requirements are met end to end: fetch Taipei JSON, page through New Taipei until a page has fewer than 1000 records, run the Overpass query with a `User-Agent` of `BikeSupplyMap/0.1 (https://github.com/fripig/BikeSupplyMap)`, then write `stations.json`, `shops.json`, `meta.json` under `public/data/`. Verify: running it against live sources exits 0 and prints per-city station counts and per-category shop counts; spot-check one Taipei and one New Taipei station and one shop per category in the output files.
- [x] 2.4 [after: 2.3] Make the pipeline enforce "Pipeline refuses to publish incomplete data": minimum counts (500 per city, 2000 shops), non-JSON and non-2xx responses fail, and all three files are written to a temp directory and renamed into `public/data/` only after every check passes. Verify: a vitest test of the count-check function covers pass and fail cases; manual run with the Overpass URL pointed at an unreachable host exits non-zero and `git diff --stat public/data` is empty.
- [x] 2.5 [after: 2.4] Commit the first generated dataset under `public/data/` and record its size. Verify: `ls -l public/data` output is noted in the task completion; if stations.json + shops.json exceed 1 MB combined, trim fields before committing (per the design risk on payload size).

## 3. Map UI (supply-map)

- [x] 3.1 [after: 1.1] Implement `haversineMeters` and `nearbyShops` in `app/utils/geo.ts` (pure, no Nuxt imports) per "Straight-line distance with fixed radius options": inclusive radius, ascending distance, category filtering by an enabled-category set. Verify: vitest reproduces the spec example (A 120 m, D 300 m, B 480 m listed; C 510 m excluded) and a category-filter case.
- [x] 3.2 [after: 1.1] Implement `directionsUrl` in `app/utils/links.ts` (pure) per "Google Maps link for directions". Verify: vitest asserts the exact URL from the spec's "generated URL" example.
- [x] 3.3 [after: 2.5] Build the base map so that "Map shows all stations" holds: client-only `SupplyMap.client.vue` rendered inside `<ClientOnly>` with OSM tiles and attribution and clustered station markers; `app/pages/index.vue` loads `data/stations.json` and `data/shops.json` prefixed with `useRuntimeConfig().app.baseURL`; Traditional Chinese UI; the `資料載入失敗，請重新整理` message when either data file fails. Verify: `npm run dev` shows clusters over both cities; temporarily renaming `public/data/shops.json` shows the failure message; `npm run generate` still succeeds (Leaflet is not evaluated during generation).
- [x] 3.4 [after: 3.1, 3.2, 3.3] Implement "Selecting a station lists nearby shops": selecting a station highlights it, renders the sorted list (name or category label, category label, distance rounded to 10 m labelled 直線距離), category-colored shop markers, the 300/500/1000 m radius control defaulting to 500 m that updates without reselecting, the empty-result message, and a Google Maps walking directions link per shop opening in a new tab. Verify: manual browser check of each `supply-map` scenario for station selection, radius change, empty result, and directions link.
- [x] 3.5 [after: 3.4] Implement the "Category filter" toggles (便利商店, 超市, 量販店, 雜貨店, all on by default) affecting both list and markers. Verify: manual check that turning off 便利商店 removes every convenience shop from the list and map while other categories stay.
- [x] 3.6 [after: 3.5] Make the page "Usable on phone screens" and add the "Data freshness notice" (data date from `meta.json` formatted in Asia/Taipei, credits to OpenStreetMap contributors and the two city open-data sources). Verify: browser at 375 px width has no horizontal page scroll and the list is reachable after selecting a station; the footer shows the date matching `meta.json`.

## 4. Deployment and refresh

- [x] 4.1 [after: 3.6] Add a deploy workflow implementing "Static site on GitHub Pages with build-time data": on push to `main`, on `workflow_dispatch`, and callable via `workflow_call`, it runs `npm ci`, `npm test`, `npm run generate`, and deploys `.output/public/` with actions/upload-pages-artifact and actions/deploy-pages; enable Pages with the GitHub Actions source. Verify: the workflow run is green and `curl -s -o /dev/null -w "%{http_code}" https://fripig.github.io/BikeSupplyMap/` prints 200.
- [x] 4.2 [after: 4.1] Add a refresh workflow implementing "Scheduled refresh commits data back to main": cron `0 19 * * 0` plus `workflow_dispatch`, runs `npm run fetch-data`, commits `public/data/` as `github-actions[bot]` only when it changed, then calls the deploy workflow via `workflow_call`. Verify: a manual `workflow_dispatch` run is green, and either a data commit plus a deploy job appear or the log states no data changed.
- [x] 4.3 [after: 4.2] Write README.md in Traditional Chinese covering purpose, `npm run dev` / `fetch-data` / `generate` / `test`, the note that SSR is possible later by switching the Nitro preset, data sources with licenses and attribution, and the refresh schedule. Verify: content review that every command listed runs as described.

## 5. Verification follow-ups

- [x] 5.1 [after: 4.3] Add automated tests for the four spec scenarios flagged by `/spectra-verify` as untested: "New Taipei pagination is exhausted" (via `fetchAllPages` in `scripts/lib/paginate.js`), "Overpass failure keeps previous data" and "Suspiciously small result is rejected" (end-to-end run of `scripts/fetch-data.js` against a local server with `DATA_DIR` pointing at seeded files), "Data fails to load" (via `loadSupplyData` in `app/utils/load-data.ts`), and "No shops within radius" plus the radius defaults (component test of `ShopList.vue`, constants test). Verify: `npx vitest run` passes, and each new test fails when the behavior it guards is removed.
- [x] 5.2 [after: 5.1] Align the implementation with the "Shop classification rules" brand order (hypermarket, supermarket, convenience) and make the write-step comment in `scripts/fetch-data.js` state exactly what is guaranteed. Verify: the classification table test passes and the category counts on the 2026-09-25 Overpass sample are unchanged.
- [x] 5.3 [after: 5.1] Record the Overpass instance fallback and the new pure modules in design.md ("Implementation Contract" and "Risks / Trade-offs"). Verify: `spectra validate add-youbike-supply-map` passes and design.md names every Overpass instance the script uses.

## 6. Review follow-ups

- [x] 6.1 [after: 5.3] Make `normalizeTaipei` and `normalizeNewTaipei` drop records whose coordinates are not finite numbers, so a renamed or empty coordinate field lowers the station count and trips the "Pipeline refuses to publish incomplete data" check instead of publishing `null`/`0` coordinates. Verify: normalizer tests for missing, empty-string and non-numeric coordinates return `null`.
- [x] 6.2 [after: 5.3] Make every source failure name the source when the response is valid JSON of the wrong shape: the Taipei body, each New Taipei page, and the Overpass `elements` field must be arrays, otherwise the script fails with `<source>: expected an array`. Verify: an end-to-end fetch-data test with a wrapped Taipei body exits non-zero, prints `Taipei YouBike: expected an array`, and leaves data byte-identical.
- [x] 6.3 [after: 5.3] Redraw shop markers once per selection or radius change in `SupplyMap.client.vue`: one watcher redraws on `nearby`, and a separate watcher on the selected station and radius only fits the map to the radius circle. Verify: in the browser, switching radius updates the markers and the view, and the redraw count per radius change is 1.
- [x] 6.4 [after: 5.3] Extend the "Category filter" behavior: when every category is turned off, the list shows `請至少選擇一種店家類型` rather than the empty-range message. Verify: a `ShopList` component test covers the all-off message, and the existing empty-range test still passes.
