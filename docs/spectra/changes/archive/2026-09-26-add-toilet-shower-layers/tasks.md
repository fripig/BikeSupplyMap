## 1. Toilet and shower dataset

- [x] 1.1 Implement facility selection distances per kind in a new scripts/lib/facilities.js: `toiletSpots(routes, elements)` and `showerSpots(routes, elements)` with `TOILET_NEAR_METERS = 100`, `SHOWER_NEAR_METERS = 300`, `SPORTS_CENTRE_NEAR_METERS = 1000`, keeping raw tag values in the data (`name`, `wheelchair`, `changing_table`, `unisex`, `fee`, or `null`), rounding to 5 decimals, deduplicating and sorting by shop-style id, and publishing an element that matches both shower rules once as `kind: "shower"`. First extract from scripts/lib/shelters.js the private `riversideSegments` as exported `riversideRouteSegments(routes)` and the inline node-or-center lookup as exported `elementPoint(element)`, and use them in both modules. Verified by a new scripts/lib/facilities.test.js covering every row of the spec's "toilet selection" and "shower selection" examples plus the double-match case, and by scripts/lib/shelters.test.js still passing (`npx vitest run scripts/lib`).
- [x] 1.2 [after: 1.1] Wire one facilities.json for both switches into scripts/fetch-data.js: add `FACILITIES_QUERY` (`amenity=toilets`, `amenity=shower`, and `leisure=sports_centre` named with 運動中心, `out center tags`) run after the shelter query; write `facilities.json` as `{"toilets":[...],"showers":[...]}` one entry per line; add `counts.toilets` and `counts.showers` to meta.json; add facility minimums in check-counts (`toilets: 150`, `showers: 5`, labels `riverside toilets` and `riverside showers`) in scripts/lib/check-counts.js with its tests; log both counts. In scripts/fetch-data.test.js the fake server routes Overpass queries by text, so it gains a `/facilities` branch matched on `"amenity"="toilets"` (the shelter branch matches `"amenity"="shelter"`, which the facility query does not contain); the SEED gains `facilities.json`; the fixture puts at least 150 toilets and 5 showers within range of the existing 7 km riverside way at lat 25.03. Verified by fetch-data tests for output and counts, rejection with 120 toilets naming `riverside toilets: got 120`, and previous data kept when the facility query fails (`npx vitest run scripts`).

## 2. Toilet and shower layers on the map

- [x] 2.1 Add raw tag values in the data, wording in the front end: `Toilet`, `Shower`, `FacilityData` types in app/utils/load-data.ts, and `toiletLabel(toilet)` and `showerLabel(shower)` in app/utils/cycling.ts. Change the legend flags as an options object: `legendEntries({ routes, urban, vending, shelters, toilets, showers })` with 廁所 and 淋浴 after the two shelter entries, updating its caller `updateLegend()` in app/components/SupplyMap.client.vue (passing `false` for toilets and showers until task 2.3) and its tests. This covers the legend of "Traffic signals and crossings along urban paths". Verified by app/utils/cycling.test.ts cases for every row of the "facility popup text" example and every row of the updated "legend entries" example (`npx vitest run app/utils`), and `npx vue-tsc --noEmit` passing.
- [x] 2.2 [after: 2.1] Add the Toilet and shower switches: MapControls.vue gains `showToilets` and `showShowers` models and `toiletFailed` and `showerFailed` props rendering `廁所` and `淋浴` switches with `廁所資料載入失敗` and `淋浴資料載入失敗` under them only on failure; app/pages/index.vue creates one `createJsonLoader<FacilityData>(fetch, app.baseURL, 'facilities.json')` shared by two `useLazyToggle(load, false)` switches, starts both on mount, and passes SupplyMap `showToilets`, `showShowers`, and `facilities` (whichever switch's data has loaded). Verified by MapControls.test.ts cases for both labels, default off, emitted values, and failure messages, and a cycling.test.ts case where two `useLazyToggle` switches sharing one `createJsonLoader` load the file once across on/off of both (`npx vitest run app`).
- [x] 2.3 [after: 2.2] Draw Toilet and shower icons as glyph icons below stations: in SupplyMap.client.vue, `facility-icon facility-icon--toilet` (`廁`, `#364fc7`) and `facility-icon facility-icon--shower` (`浴`, `#c2255c`) divIcons with `zIndexOffset: -1000`, popups and titles from `toiletLabel` and `showerLabel`, one layer group per kind added or removed by its switch, legend flags passed from those layers, and legend glyph styles. Verified by `npx vue-tsc --noEmit` passing and task 3.2's e2e tests.

## 3. Data, browser checks and docs

- [x] 3.1 [after: 1.2] Regenerate data with `npm run fetch-data`; the run succeeds and public/data/facilities.json holds at least 150 toilets and 5 showers, including a `sports_centre` named `萬華運動中心`. Verified by the command output and a node check of facilities.json.
- [x] 3.2 [after: 2.3, 3.1] Add e2e tests in e2e/map.spec.ts: no request for `data/facilities.json` at load; turning 廁所 on draws only toilet icons and adds 廁所 to the legend, then turning 淋浴 on adds shower icons and 淋浴 without a second request; clicking a toilet icon clear of stations shows `toiletLabel` of its data; with `data/facilities.json` returning 404, turning 廁所 on shows `廁所資料載入失敗`, leaves the switch off, and a riverside station stays selectable. Verified by `npm run test:e2e` passing on desktop and phone.
- [x] 3.3 [after: 3.1] Document the 廁所 and 淋浴 layers in README.md: what each shows, that both are off by default and share one download, selection rules and their module, that sports centre showers are likely paid and OSM showers are scarce, the popup attributes, and the new fetch-data minimums. Verified by reading the README diff against the toilet and shower dataset requirement.
