## 1. Station switches

- [x] 1.1 Deliver Map shows riverside stations by default with two station switches instead of a master switch: MapControls.vue gains a required `showRiverside` model rendered as a `河濱站點` switch before the urban one, and the urban switch label becomes `市區站點`; index.vue holds `showRiverside = ref(true)`, binds it to MapControls and SupplyMap, and its hint names `市區站點`; SupplyMap.client.vue keeps the riverside cluster group in a module variable, adds it on mount only while `showRiverside` is true, and adds or removes it in a watcher, so the riverside switch mirrors the urban one. The `都市自行車道` toggle stays independent of both station switches, keeping the Urban bike-path layer toggle requirement. Verified by MapControls.test.ts cases for both labels, default states, and emitted `update:showRiverside` / `update:showUrban` values (`npx vitest run app/components`), and `npx vue-tsc --noEmit` passing.
- [x] 1.2 [after: 1.1] Cover the station visibility example and selection survival in e2e/map.spec.ts: on load `河濱站點` is checked and `市區站點` unchecked; after turning `河濱站點` off, no riverside station title is on the map; after turning `市區站點` on, urban station markers appear; a riverside station selected before turning `河濱站點` off keeps its `.station-name` and shop list. Update README.md to describe both switches. Verified by `npm run test:e2e`.

## 2. Google Maps location link

- [x] 2.1 Add the location link URL using Google Maps search: `mapsPlaceUrl(place)` in app/utils/links.ts returning `https://www.google.com/maps/search/?api=1&query=<lat>,<lng>`. Verified by app/utils/links.test.ts cases for both rows of the "generated URL" example (`npx vitest run app/utils`).
- [x] 2.2 [after: 1.2, 2.1] Deliver the Google Maps location link using popup markup with an escaped label, line break, and link: a `placePopup(label, place)` helper in SupplyMap.client.vue used by the vending, shelter, and glyph-layer popups (titles unchanged, route line popups unchanged), and the station link in the side panel of index.vue under `.station-meta` with class `station-link`, `target="_blank"` and `rel="noopener"`. Update README.md to mention the link. Verified by e2e tests: the vending, shelter, and toilet popup tests check the text before the link and a `在 Google 地圖開啟` link whose href is `mapsPlaceUrl` of that icon and whose target is `_blank`; a selected station shows the link with its coordinates and the link changes when another station is selected (`npm run test:e2e`); plus `npx vitest run` and `npx vue-tsc --noEmit` passing.
