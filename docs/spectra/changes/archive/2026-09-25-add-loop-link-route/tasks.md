## 1. Link routes in the bike route dataset

- [x] 1.1 Add the loop route list keyed by relation name: scripts/lib/loop-routes.js exports `LOOP_ROUTES` whose first entry is `{ name: '環騎臺北', label: '環騎臺北（連接道路）' }`. Verified by a new test in scripts/lib/routes.test.js asserting that first entry.
- [x] 1.2 [after: 1.1] Extend `buildRoutes(body, supplements, loops)` in scripts/lib/routes.js to subtract riverside and bridge ways by way id and publish each loop entry as a `kind: "link"` route named by its `label`, with link routes placed after supplementary bridges, and to return `missingLoops` for an entry with no matching relation or no way left. This delivers the Bike route dataset link rules. Verified by new cases in scripts/lib/routes.test.js (`npx vitest run scripts/lib/routes.test.js`): riverside and bridge member ways dropped, a way shared only with an unpublished relation kept, route order relation → supplementary bridge → link, `missingLoops` for an absent relation and for a fully-subtracted relation, and the existing `buildRoutes` cases still passing with no `loops` argument.
- [x] 1.3 [after: 1.2] Confirm route-side vending covers link lines: a new `routeVending` case in scripts/lib/routes.test.js places a vending shop 120 m from a link line and more than 200 m from every other line and expects it in the result. Verified by `npx vitest run scripts/lib/routes.test.js`.
- [x] 1.4 [after: 1.2] Wire `LOOP_ROUTES` into scripts/fetch-data.js: pass it to `buildRoutes`, turn every `missingLoops` name into the problem `loop route <name>: no relation with this name, or no way left outside riverside and bridge routes` so publishing is refused, and log the link route count. Riverside station classification keeps using `riversideSegments()` only, so link lines do not mark stations riverside (link kind excluded from riverside classification). The shared `routesBody` fixture in scripts/fetch-data.test.js currently has no `環騎臺北` relation, so it gains one (a relation named `環騎臺北` with one way of its own, far from every fake station) behind a flag, as `supplement` does for 重陽橋, keeping the existing success cases green. Verified by a new case where the flag is off and the run exits non-zero naming `環騎臺北` with `public/data/` untouched, the existing route case asserting routes.json now ends with the link route, and `npx vitest run scripts`.

## 2. Map drawing and legend

- [x] 2.1 Widen `BikeRoute['kind']` in app/utils/load-data.ts to `'riverside' | 'bridge' | 'link'` and add the 連接道路 legend entry after 橋梁自行車道 in `legendEntries()` (app/utils/cycling.ts), satisfying the legend part of "Traffic signals and crossings along urban paths". Verified by updating the `legendEntries` table in app/utils/cycling.test.ts to the spec's legend example rows and running `npx vitest run app/utils/cycling.test.ts`, plus `npx vue-tsc --noEmit` passing.
- [x] 2.2 [after: 2.1] Draw link routes in a third route color with popup, completing "Riverside and bridge routes are drawn": in `drawRoutes()` (app/components/SupplyMap.client.vue) link lines use `LINK_COLOR = '#9c6644'`, weight 6, opacity 0.9, are interactive and bind a popup with the route name; add the `.cycling-legend__line--link` style. Verified by task 3.2's e2e test.

## 3. Data refresh and end-to-end check

- [x] 3.1 [after: 1.4] Regenerate data with `npm run fetch-data`; the run succeeds and public/data/routes.json contains exactly one route with `kind: "link"` and name `環騎臺北（連接道路）` whose lines pass through 研究院路 (a point near 25.036, 121.617). Verified by the command output and a jq/node check of routes.json.
- [x] 3.2 [after: 2.2, 3.1] Add an e2e test in e2e/map.spec.ts, modelled on the 重陽橋 test, that finds the `環騎臺北（連接道路）` route through `loadRoutes`, clicks the middle of its longest line at zoom 17, and expects the popup text `環騎臺北（連接道路）`; also assert the legend contains `連接道路`. Verified by `npm run test:e2e` passing.
