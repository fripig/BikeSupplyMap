## 1. Riverside classification in the data pipeline

- [x] 1.1 Riverside station classification, route part — Select riverside routes by relation name: add a module under scripts/lib/ exporting `isRiversideRoute(tags)` (name matches `河|溪|水岸|左岸|右岸`, or equals `關渡自行車道`, `社子島環島自行車道`, `二重環狀自行車道`; nameless relations are rejected) and a function that turns an Overpass `out geom` response into line segments from the member ways of selected relations. Verify: a Vitest test covering every row of the "route names" example table in the supply-data delta spec passes under `npm test`.
- [x] 1.2 Riverside station classification, distance part — Measure station-to-path distance with a flat projection: add `classifyRiverside(stations, segments, thresholdMeters = 200)` returning stations with boolean `riverside`, using the nearest point on each segment clamped to the end points and the 0.01° latitude prefilter. Verify: a Vitest test covering the four rows of the "distance boundary" example table (189 m true, 211 m false, 151 m past the end true, 252 m past the end false) passes under `npm test`. [after: 1.1]
- [x] 1.3 Pipeline refuses to publish incomplete data — Guard riverside counts in the publish check: `checkCounts` in scripts/lib/check-counts.js gains minimums `riversideRoutes: 15` and `riversideStations: 150`, with labels naming riverside routes and riverside stations. Verify: new cases in scripts/lib/check-counts.test.js assert that 4 riverside routes yields one message containing the label and the count 4, and that counts at the minimums yield no message.
- [x] 1.4 Fetch riverside routes with a second Overpass query and Store the riverside flag on each station record: in scripts/fetch-data.js, make the Overpass fetch take its query as a parameter, run the route query next to the shop query, classify the merged stations, pass `riversideRoutes` and `riversideStations` to `checkCounts`, and write `counts.riversideStations` to meta.json. This satisfies "Station dataset covers both cities" with the `riverside` field on every record. Verify: `npm run fetch-data` succeeds and prints the riverside station count; `node -e` over public/data/stations.json confirms every record has a boolean `riverside`, and meta.json shows `counts.riversideStations` equal to the printed count. [after: 1.2, 1.3]

## 2. Map shows riverside stations by default

- [x] 2.1 Two station layers on the map: `Station` in app/utils/geo.ts gains `riverside: boolean`; SupplyMap.client.vue puts riverside stations in the existing cluster group and urban stations in a second cluster group with a muted grey, smaller marker, added or removed when a new `showUrban` prop changes, without clearing the selected-station highlight, radius circle, or shop markers. Verify: `npx nuxi typecheck` reports no errors, and the manual check in 2.4 passes. [after: 1.4]
- [x] 2.2 Toggle lives in MapControls and state in the page: MapControls.vue gains a `v-model:show-urban` switch labelled `顯示市區站點`, off by default and operable by touch; index.vue holds the state, passes it to `SupplyMap`, and changes the hint to point at riverside stations and mention the toggle. Verify: the manual check in 2.4 passes. [after: 2.1]
- [x] 2.3 Initial view fits riverside stations: on mount, SupplyMap.client.vue fits the map to the riverside stations' bounds, and falls back to the previous fixed Taipei view when there are none. Verify: the manual check in 2.4 passes. [after: 2.1]
- [x] 2.4 Manual check of "Map shows riverside stations by default" in `npm run dev`, on desktop and at 375 px width: on load only riverside stations appear, framed across both cities, and the toggle is off; turning it on adds grey urban stations and turning it off removes them; selecting an urban station and then turning the toggle off keeps its highlight, list, and shop markers. Record the result in the task notes. [after: 2.2, 2.3]
  - Result (2026-09-25, `npm run dev`, Chrome): on load only riverside clusters shown, framed across both cities (cluster counts 31+95+56+4+123+15 plus 2 single markers = 326), toggle off. Toggle on → 93 urban clusters + 25 grey markers; off → 0, riverside clusters unchanged at 34. Selected urban station 雙溪車站 (4 shops), toggled off → heading, 「附近 4 家」, selected icon and 4 shop markers kept. 375 px iframe: document scrollWidth 360, switch row 32 px tall, controls / map / hint stacked.

## 3. Docs and release

- [x] 3.1 README feature list describes riverside stations as the default view, the 200 m rule based on OSM riverside bike-path routes, and the `顯示市區站點` toggle. Verify: content review of README.md. [after: 2.4]
- [x] 3.2 Regenerated public/data/ ships in the same commit as the UI change, as the design's migration plan requires. Verify: `npm test` and `npm run generate` both pass, and `git show --stat` for the commit lists public/data/stations.json together with the app/ changes. [after: 3.1]
